{
  "name": "packfile-viewer",
  "version": "0.1.0",
  "private": true,
  "main": "dist/app.js",
  "scripts": {
    "build": "node scripts/build.js",
    "compile": "tsc --build tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ross Light",
  "dependencies": {
    "@hotwired/turbo": "^7.0.0-beta.1",
    "esbuild": "^0.8.26",
    "stimulus": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
